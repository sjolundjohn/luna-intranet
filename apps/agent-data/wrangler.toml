# agent-data — placeholder for the Data Agent Worker. Status:
# coming-soon. Wired so the agent-router service binding resolves;
# returns 503 with the canonical "coming-soon" message until Phase 2+.
name = "luna-agent-data"
main = "src/index.ts"
compatibility_date = "2026-05-01"
compatibility_flags = ["nodejs_compat"]
workers_dev = false
preview_urls = false

[observability]
enabled = true
